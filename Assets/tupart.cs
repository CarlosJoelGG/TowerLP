using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tupart : MonoBehaviour
{
    public tutorial sigu;
    // Start is called before the first frame update
    void Start()
    {
        sigu.siguientepaso();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
